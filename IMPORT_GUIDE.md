## VBAPlaywright xlsm 导入指南# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2.# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 →# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.x# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

#### VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器
# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBA# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2.# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3.# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：
# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 |# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriver# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 |# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls`# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` |# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PW# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4.# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行
# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_B# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VB# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}
# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWork# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
c# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 V# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1.# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/You# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/YouQiangSun/VBAPlaywright)
2. 点击 **Code → Download ZIP**
3. 解压后得到所有 `.bas` 和 `.cls` 文件
4. 按方法一导入到 Excel

---

## 运行环境要求

- **操作系统**：Windows（因为 WebDriver 是# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/YouQiangSun/VBAPlaywright)
2. 点击 **Code → Download ZIP**
3. 解压后得到所有 `.bas` 和 `.cls` 文件
4. 按方法一导入到 Excel

---

## 运行环境要求

- **操作系统**：Windows（因为 WebDriver 是 Windows 可执行文件）
- **# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/YouQiangSun/VBAPlaywright)
2. 点击 **Code → Download ZIP**
3. 解压后得到所有 `.bas` 和 `.cls` 文件
4. 按方法一导入到 Excel

---

## 运行环境要求

- **操作系统**：Windows（因为 WebDriver 是 Windows 可执行文件）
- **Microsoft Excel**：2010 或更高版本（# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/YouQiangSun/VBAPlaywright)
2. 点击 **Code → Download ZIP**
3. 解压后得到所有 `.bas` 和 `.cls` 文件
4. 按方法一导入到 Excel

---

## 运行环境要求

- **操作系统**：Windows（因为 WebDriver 是 Windows 可执行文件）
- **Microsoft Excel**：2010 或更高版本（支持 .xlsm）
- **# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/YouQiangSun/VBAPlaywright)
2. 点击 **Code → Download ZIP**
3. 解压后得到所有 `.bas` 和 `.cls` 文件
4. 按方法一导入到 Excel

---

## 运行环境要求

- **操作系统**：Windows（因为 WebDriver 是 Windows 可执行文件）
- **Microsoft Excel**：2010 或更高版本（支持 .xlsm）
- **浏览器**：已安装 Chrome / Edge / Firefox
# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/YouQiangSun/VBAPlaywright)
2. 点击 **Code → Download ZIP**
3. 解压后得到所有 `.bas` 和 `.cls` 文件
4. 按方法一导入到 Excel

---

## 运行环境要求

- **操作系统**：Windows（因为 WebDriver 是 Windows 可执行文件）
- **Microsoft Excel**：2010 或更高版本（支持 .xlsm）
- **浏览器**：已安装 Chrome / Edge / Firefox
- **PowerShell**：Windows 10+# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/YouQiangSun/VBAPlaywright)
2. 点击 **Code → Download ZIP**
3. 解压后得到所有 `.bas` 和 `.cls` 文件
4. 按方法一导入到 Excel

---

## 运行环境要求

- **操作系统**：Windows（因为 WebDriver 是 Windows 可执行文件）
- **Microsoft Excel**：2010 或更高版本（支持 .xlsm）
- **浏览器**：已安装 Chrome / Edge / Firefox
- **PowerShell**：Windows 10+ 自带（用于解压 zip，驱动下载）

## 常见问题

### Q:# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/YouQiangSun/VBAPlaywright)
2. 点击 **Code → Download ZIP**
3. 解压后得到所有 `.bas` 和 `.cls` 文件
4. 按方法一导入到 Excel

---

## 运行环境要求

- **操作系统**：Windows（因为 WebDriver 是 Windows 可执行文件）
- **Microsoft Excel**：2010 或更高版本（支持 .xlsm）
- **浏览器**：已安装 Chrome / Edge / Firefox
- **PowerShell**：Windows 10+ 自带（用于解压 zip，驱动下载）

## 常见问题

### Q: 导入时提示"无法导入文件"
A: 确保文件编码为 UTF-8# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/YouQiangSun/VBAPlaywright)
2. 点击 **Code → Download ZIP**
3. 解压后得到所有 `.bas` 和 `.cls` 文件
4. 按方法一导入到 Excel

---

## 运行环境要求

- **操作系统**：Windows（因为 WebDriver 是 Windows 可执行文件）
- **Microsoft Excel**：2010 或更高版本（支持 .xlsm）
- **浏览器**：已安装 Chrome / Edge / Firefox
- **PowerShell**：Windows 10+ 自带（用于解压 zip，驱动下载）

## 常见问题

### Q: 导入时提示"无法导入文件"
A: 确保文件编码为 UTF-8 或 ANSI。如果从 GitHub 下载，# VBAPlaywright xlsm 导入指南

## 方法一：手动导入（推荐，最稳定）

### 1. 创建 xlsm 文件

1. 打开 Microsoft Excel
2. 新建空白工作簿
3. **文件 → 另存为**
4. 选择类型：**Excel 启用宏的工作簿 (*.xlsm)**
5. 保存为 `VBAPlaywright.xlsm`

### 2. 打开 VBA 编辑器

按 `Alt + F11` 打开 VBA 编辑器。

### 3. 导入模块和类

在 VBA 编辑器左侧的"工程资源管理器"中：

1. 右键点击你的项目（如 `VBAProject (VBAPlaywright.xlsm)`）
2. 选择 **导入文件...**
3. 按以下顺序导入所有文件：

| 顺序 | 文件名 | 类型 |
|---|---|---|
| 1 | `WebDriverCore.bas` | 标准模块 |
| 2 | `WebDriverUpdater.bas` | 标准模块 |
| 3 | `Playwright.bas` | 标准模块 |
| 4 | `TestReporter.bas` | 标准模块 |
| 5 | `Tests.bas` | 标准模块 |
| 6 | `Example.bas` | 标准模块 |
| 7 | `Browser.cls` | 类模块 |
| 8 | `Page.cls` | 类模块 |
| 9 | `Locator.cls` | 类模块 |
| 10 | `PWElement.cls` | 类模块 |

> 导入顺序建议：先导入 `.bas` 标准模块，再导入 `.cls` 类模块。

### 4. 运行测试

在 VBA 编辑器中：

1. 打开 `Tests` 模块
2. 将光标放在 `RunAllTests` 过程中
3. 按 `F5` 运行

或者在立即窗口（`Ctrl + G`）输入：
```vba
Tests.RunAllTests
```

也可以运行单个测试：
```vba
Tests.Test_BasicNavigation
```

### 5. 运行示例

打开 `Example` 模块，运行任意一个示例：
```vba
Demo_BasicSearch
```

---

## 方法二：PowerShell 自动创建（Windows 上需要安装 Excel）

如果你安装了 Microsoft Excel，可以使用以下 PowerShell 脚本自动创建 xlsm 文件并导入所有模块：

```powershell
# Create-Xlsm.ps1
# 在代码目录下运行此脚本

$excel = New-Object -ComObject Excel.Application
$excel.Visible = $false
$wb = $excel.Workbooks.Add()

# 导入所有 bas 文件
Get-ChildItem *.bas | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

# 导入所有 cls 文件
Get-ChildItem *.cls | ForEach-Object {
    $wb.VBProject.VBComponents.Import($_.FullName)
}

$savePath = Join-Path (Get-Location) "VBAPlaywright.xlsm"
$wb.SaveAs($savePath, 52)  # 52 = xlOpenXMLWorkbookMacroEnabled
$wb.Close()
$excel.Quit()

Write-Host "Created: $savePath"
```

运行方式（以管理员身份打开 PowerShell）：
```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
cd C:\Path\To\VBAPlaywright
.\Create-Xlsm.ps1
```

> **注意**：Excel 的宏安全设置可能需要调整，允许访问 VBA 项目对象模型。

---

## 方法三：从 GitHub 直接下载代码包

1. 访问 [github.com/YouQiangSun/VBAPlaywright](https://github.com/YouQiangSun/VBAPlaywright)
2. 点击 **Code → Download ZIP**
3. 解压后得到所有 `.bas` 和 `.cls` 文件
4. 按方法一导入到 Excel

---

## 运行环境要求

- **操作系统**：Windows（因为 WebDriver 是 Windows 可执行文件）
- **Microsoft Excel**：2010 或更高版本（支持 .xlsm）
- **浏览器**：已安装 Chrome / Edge / Firefox
- **PowerShell**：Windows 10+ 自带（用于解压 zip，驱动下载）

## 常见问题

### Q: 导入时提示"无法导入文件"
A: 确保文件编码为 UTF-8 或 ANSI。如果从 GitHub 下载，通常不会有编码问题。

### Q: